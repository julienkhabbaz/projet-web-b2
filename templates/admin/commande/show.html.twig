{% extends 'admin.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Ref_commande</th>
                <td>{{ commande.refCommande }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ commande.status ? 'Passées' : 'En cours' }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ commande.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_index') }}">retour a la liste </a>

    <a href="{{ path('commande_edit', {'id': commande.id}) }}">Modifier</a>

    {{ include('admin/commande/_delete_form.html.twig') }}
{% endblock %}
